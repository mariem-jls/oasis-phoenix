# Model Files

Place the trained model artifacts in this folder so the project works after clone.

Required files:
- isolation_forest.pkl
- random_forest.pkl
- ridge_regression.pkl
- scaler.pkl
- scaler_ridge.pkl

The app loads them from services/model_loader.py at startup.
